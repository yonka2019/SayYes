# Localization (Hebrew / Russian / English) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship SayYes in Hebrew, Russian, and English with a visible language switcher and server-side auto-detection, without ever machine-translating creator-authored invitation content.

**Architecture:** A hand-rolled `src/lib/i18n/` module holds three typed dictionaries (`he` is the source of truth, so a missing key in `ru`/`en` is a compile error) plus a pure `resolveLocale()` that implements the detection precedence URL prefix → cookie → `Accept-Language` → `he`. `src/middleware.ts` runs that resolution before render and redirects prefix-less requests. Every page moves under `src/app/[locale]/`, whose layout becomes the root layout and owns `<html lang dir>`. Invitations store the locale they were authored in, and the invite page redirects to it so content and chrome always agree.

**Tech Stack:** Next.js 15 App Router, React 19, TypeScript, Prisma + Postgres, Tailwind v4, Vitest, `next/font/google` (Rubik).

**Spec:** `docs/superpowers/specs/2026-07-26-i18n-design.md`

## Global Constraints

- **No bare user-facing string literals in components or routes.** Every chrome string resolves through `t(dict, key)`. This replaces the old "Hebrew only" rule.
- **Creator-authored content is never translated:** `recipientName`, `gateQuestion`, `Question.text`, `QuestionOption.label`.
- `he` is `DEFAULT_LOCALE` and the source-of-truth dictionary. `LOCALES = ["he", "ru", "en"]`.
- Direction: `he` → `rtl`, `ru` → `ltr`, `en` → `ltr`.
- Cookie name is exactly `sayyes_locale`, `Max-Age` 31536000, `SameSite=Lax`, `Path=/`.
- Answers stay multiple choice, 2–4 options: `MIN_OPTIONS = 2`, `MAX_OPTIONS = 4` unchanged.
- Length limits unchanged: `MAX_NAME_LENGTH = 40`, `MAX_TEXT_LENGTH = 120`, `MAX_OPTION_LENGTH = 40`.
- Mascot still has no default — the creator must actively pick `BEAR` or `PENGUIN`.
- Dodge behaviour is unchanged: `PROXIMITY_RADIUS = 130`, flee 55–110px, hop 20–50px, `SHRINK_PER_CATCH = 0.82`, `MIN_SCALE = 0.3`, last plea repeats forever.
- Next 15: route `params` is a `Promise` — always `await` it, including the new `locale`.
- Use the theme tokens (`blush`, `blush-deep`, `rose-deep`, `rose-soft`, `rose-ink`), never raw hex.
- Phone width 390px is the primary viewport. After layout changes, check `document.body.scrollWidth <= window.innerWidth`.
- **Commits are deferred.** The repo owner's standing rule is never to commit or push without an explicit request. Each task ends with a prepared commit message; run it only when the owner asks. Do not `git push` at all.
- `npm test` and `npm run build` must both pass at the end of every task.

## File Structure

**Created**
| File | Responsibility |
|---|---|
| `src/lib/i18n/locales.ts` | Locale constants, direction map, endonyms, date-locale map, and the pure detection/path functions. No React, no Next imports. |
| `src/lib/i18n/dictionaries/he.ts` | Source-of-truth string table. |
| `src/lib/i18n/dictionaries/ru.ts` | Russian table, typed `: Dictionary`. |
| `src/lib/i18n/dictionaries/en.ts` | English table, typed `: Dictionary`. |
| `src/lib/i18n/t.ts` | `Dictionary` type, `getDictionary()`, `format()`, `t()`. |
| `src/middleware.ts` | Wires `resolveLocale()` to the request; redirects prefix-less paths; sets the cookie. |
| `src/components/LanguageSwitcher.tsx` | Three endonym pills; writes the cookie and swaps the path prefix. |
| `src/app/[locale]/layout.tsx` | New **root** layout: `<html lang dir>`, Rubik font, metadata per locale. |
| `src/app/[locale]/page.tsx` | Dashboard (moved). |
| `src/app/[locale]/new/page.tsx` | Builder page (moved). |
| `src/app/[locale]/invite/[token]/page.tsx` | Invite page (moved), plus the invitation-locale redirect. |
| `tests/i18n.test.ts` | Detection, path helpers, dictionary integrity. |

**Modified**
| File | Change |
|---|---|
| `prisma/schema.prisma` | `Invitation.locale String @default("he")`. |
| `src/lib/types.ts` | `FieldError`/`ErrorCode`; `DraftErrors` carries codes; `Draft` and `InviteView` carry `locale`. |
| `src/lib/validation.ts` | Return codes instead of Hebrew sentences. |
| `src/lib/dodge.ts` | `PLEAS` → `PLEA_KEYS`; `pleaForCatches` → `pleaKeyForCatches`. |
| `src/lib/defaults.ts` | Seeds become `defaultGateQuestion(locale)` / `defaultQuestions(locale)`. |
| `src/app/api/invitations/route.ts` | Accept + persist `locale`; return error codes. |
| `src/app/api/invitations/[token]/answers/route.ts` | Return error codes. |
| `src/app/globals.css` | `--font-varela` → `--font-app`. |
| `src/components/BuilderForm.tsx` | Dictionary-driven; locale-prefixed share link; renders error codes. |
| `src/components/DashboardList.tsx` | Dictionary-driven; locale badge; locale-prefixed links. |
| `src/components/InviteFlow.tsx` | Dictionary-driven. |
| `src/components/RecapCard.tsx` | `title` becomes required. |
| `src/components/Mascot.tsx` | `label` becomes a required prop. |
| `src/components/DodgeButton.tsx` | Takes `pleas: readonly string[]`. |
| `CLAUDE.md`, `README.md` | Replace the "Hebrew only" rule; document the i18n system. |

**Deleted**: `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/new/page.tsx`, `src/app/invite/[token]/page.tsx` (all moved under `[locale]/`). `src/app/icon.svg` and `src/app/api/**` stay where they are.

---

### Task 1: Locale primitives and detection

**Files:**
- Create: `src/lib/i18n/locales.ts`
- Test: `tests/i18n.test.ts`

**Interfaces:**
- Consumes: nothing.
- Produces:
  ```ts
  export const LOCALES: readonly ["he", "ru", "en"];
  export type Locale = "he" | "ru" | "en";
  export const DEFAULT_LOCALE: Locale;
  export const LOCALE_COOKIE = "sayyes_locale";
  export const DIR: Record<Locale, "rtl" | "ltr">;
  export const LOCALE_NAMES: Record<Locale, string>;
  export const DATE_LOCALE: Record<Locale, string>;
  export function isLocale(value: unknown): value is Locale;
  export function localeFromPath(pathname: string): { locale: Locale | null; rest: string };
  export function swapLocale(pathname: string, next: Locale): string;
  export function negotiate(acceptLanguage: string | null | undefined): Locale | null;
  export function resolveLocale(input: { pathname: string; cookie?: string | null; acceptLanguage?: string | null }): Locale;
  ```

- [ ] **Step 1: Write the failing tests**

Create `tests/i18n.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import {
  DEFAULT_LOCALE,
  DIR,
  isLocale,
  localeFromPath,
  negotiate,
  resolveLocale,
  swapLocale,
} from "@/lib/i18n/locales";

describe("isLocale", () => {
  it("accepts the three shipped locales", () => {
    expect(isLocale("he")).toBe(true);
    expect(isLocale("ru")).toBe(true);
    expect(isLocale("en")).toBe(true);
  });

  it("rejects anything else", () => {
    expect(isLocale("de")).toBe(false);
    expect(isLocale("")).toBe(false);
    expect(isLocale(null)).toBe(false);
    expect(isLocale(7)).toBe(false);
  });
});

describe("localeFromPath", () => {
  it("splits a prefixed path into locale and remainder", () => {
    expect(localeFromPath("/ru/new")).toEqual({ locale: "ru", rest: "/new" });
    expect(localeFromPath("/he/invite/abc123")).toEqual({
      locale: "he",
      rest: "/invite/abc123",
    });
  });

  it("treats a bare locale as the root of that locale", () => {
    expect(localeFromPath("/en")).toEqual({ locale: "en", rest: "/" });
    expect(localeFromPath("/en/")).toEqual({ locale: "en", rest: "/" });
  });

  it("reports no locale for an unprefixed path", () => {
    expect(localeFromPath("/")).toEqual({ locale: null, rest: "/" });
    expect(localeFromPath("/new")).toEqual({ locale: null, rest: "/new" });
    expect(localeFromPath("/de/new")).toEqual({ locale: null, rest: "/de/new" });
  });
});

describe("swapLocale", () => {
  it("replaces an existing prefix", () => {
    expect(swapLocale("/he/new", "ru")).toBe("/ru/new");
    expect(swapLocale("/he", "en")).toBe("/en");
  });

  it("adds a prefix when there is none", () => {
    expect(swapLocale("/new", "he")).toBe("/he/new");
    expect(swapLocale("/", "ru")).toBe("/ru");
  });

  it("round-trips", () => {
    expect(swapLocale(swapLocale("/he/invite/x", "en"), "he")).toBe("/he/invite/x");
  });
});

describe("negotiate", () => {
  it("matches on the base subtag", () => {
    expect(negotiate("ru-RU")).toBe("ru");
    expect(negotiate("he-IL")).toBe("he");
    expect(negotiate("en-GB")).toBe("en");
  });

  it("maps the legacy Hebrew tag iw to he", () => {
    expect(negotiate("iw-IL")).toBe("he");
    expect(negotiate("iw")).toBe("he");
  });

  it("honours q-weights rather than list order", () => {
    expect(negotiate("de;q=1.0, ru;q=0.9, en;q=0.8")).toBe("ru");
    expect(negotiate("en;q=0.4, he;q=0.9")).toBe("he");
  });

  it("defaults a missing q to 1", () => {
    expect(negotiate("en, ru;q=0.9")).toBe("en");
  });

  it("skips languages it does not ship and keeps looking", () => {
    expect(negotiate("fr-FR, de-DE, en-US")).toBe("en");
  });

  it("returns null when nothing matches or the header is unusable", () => {
    expect(negotiate("fr,de")).toBeNull();
    expect(negotiate("")).toBeNull();
    expect(negotiate(null)).toBeNull();
    expect(negotiate(";;;q=")).toBeNull();
  });

  it("ignores the wildcard rather than treating it as a match", () => {
    expect(negotiate("*")).toBeNull();
  });
});

describe("resolveLocale", () => {
  it("prefers an explicit path prefix over everything", () => {
    expect(
      resolveLocale({ pathname: "/en/new", cookie: "ru", acceptLanguage: "he-IL" })
    ).toBe("en");
  });

  it("falls back to the cookie when the path has no prefix", () => {
    expect(
      resolveLocale({ pathname: "/new", cookie: "ru", acceptLanguage: "he-IL" })
    ).toBe("ru");
  });

  it("ignores a cookie that is not a shipped locale", () => {
    expect(
      resolveLocale({ pathname: "/new", cookie: "de", acceptLanguage: "he-IL" })
    ).toBe("he");
  });

  it("falls back to the header when there is no cookie", () => {
    expect(
      resolveLocale({ pathname: "/", cookie: null, acceptLanguage: "ru-RU,ru;q=0.9" })
    ).toBe("ru");
  });

  it("falls back to Hebrew when nothing is usable", () => {
    expect(resolveLocale({ pathname: "/" })).toBe(DEFAULT_LOCALE);
    expect(resolveLocale({ pathname: "/", cookie: null, acceptLanguage: "fr" })).toBe("he");
  });
});

describe("DIR", () => {
  it("only Hebrew is right-to-left", () => {
    expect(DIR).toEqual({ he: "rtl", ru: "ltr", en: "ltr" });
  });
});
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `npm test -- tests/i18n.test.ts`
Expected: FAIL — `Failed to resolve import "@/lib/i18n/locales"`.

If instead the failure is that `@/` does not resolve at all, add the alias to `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  resolve: { alias: { "@": path.resolve(__dirname, "src") } },
  test: { environment: "node" },
});
```

(Check the existing file first — the other tests already import from `@/lib/...`, so the alias is most likely present. Keep whatever `test` options are already there.)

- [ ] **Step 3: Write the implementation**

Create `src/lib/i18n/locales.ts`:

```ts
/**
 * Locale primitives and detection. Deliberately dependency-free — the
 * middleware, server components and client components all import this, and the
 * detection rules are unit tested without a request.
 */

export const LOCALES = ["he", "ru", "en"] as const;
export type Locale = (typeof LOCALES)[number];

export const DEFAULT_LOCALE: Locale = "he";

/** Set on the first negotiated response, and on every manual switch. */
export const LOCALE_COOKIE = "sayyes_locale";
export const LOCALE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;

export const DIR: Record<Locale, "rtl" | "ltr"> = { he: "rtl", ru: "ltr", en: "ltr" };

/** Endonyms — a language is always offered in its own script. */
export const LOCALE_NAMES: Record<Locale, string> = {
  he: "עברית",
  ru: "Русский",
  en: "English",
};

/** BCP-47 tags for Intl.DateTimeFormat. */
export const DATE_LOCALE: Record<Locale, string> = {
  he: "he-IL",
  ru: "ru-RU",
  en: "en-US",
};

/** `iw` is the deprecated ISO code for Hebrew, still sent by some browsers. */
const ALIASES: Record<string, Locale> = { iw: "he" };

export function isLocale(value: unknown): value is Locale {
  return typeof value === "string" && (LOCALES as readonly string[]).includes(value);
}

/** Split `/ru/new` into `{ locale: "ru", rest: "/new" }`. */
export function localeFromPath(pathname: string): { locale: Locale | null; rest: string } {
  const match = /^\/([^/]+)(\/.*)?$/.exec(pathname);
  if (!match || !isLocale(match[1])) return { locale: null, rest: pathname };

  const rest = match[2] && match[2] !== "/" ? match[2] : "/";
  return { locale: match[1], rest };
}

/** Same path, different locale prefix. Adds one if the path has none. */
export function swapLocale(pathname: string, next: Locale): string {
  const { rest } = localeFromPath(pathname);
  return rest === "/" ? `/${next}` : `/${next}${rest}`;
}

/**
 * Best shipped locale for an `Accept-Language` header, honouring q-weights.
 * Returns null when the visitor asked for nothing we have.
 */
export function negotiate(acceptLanguage: string | null | undefined): Locale | null {
  if (!acceptLanguage) return null;

  const ranked = acceptLanguage
    .split(",")
    .map((part) => {
      const [tag, ...params] = part.trim().split(";");
      const q = params
        .map((param) => /^\s*q=([\d.]+)\s*$/.exec(param))
        .find((found) => found !== null);
      return { tag: tag.trim().toLowerCase(), q: q ? Number(q[1]) : 1 };
    })
    .filter((entry) => entry.tag.length > 0 && !Number.isNaN(entry.q) && entry.q > 0)
    .sort((a, b) => b.q - a.q);

  for (const { tag } of ranked) {
    const base = tag.split("-")[0];
    const resolved = ALIASES[base] ?? base;
    if (isLocale(resolved)) return resolved;
  }
  return null;
}

/**
 * The one detection rule for the whole app:
 * URL prefix → cookie → Accept-Language → `he`.
 */
export function resolveLocale({
  pathname,
  cookie,
  acceptLanguage,
}: {
  pathname: string;
  cookie?: string | null;
  acceptLanguage?: string | null;
}): Locale {
  const fromPath = localeFromPath(pathname).locale;
  if (fromPath) return fromPath;
  if (isLocale(cookie)) return cookie;
  return negotiate(acceptLanguage) ?? DEFAULT_LOCALE;
}
```

- [ ] **Step 4: Run the tests to verify they pass**

Run: `npm test -- tests/i18n.test.ts`
Expected: PASS, all cases.

- [ ] **Step 5: Prepared commit (deferred — run only when the owner asks)**

```bash
git add src/lib/i18n/locales.ts tests/i18n.test.ts vitest.config.ts
git commit -m "feat(i18n): add locale primitives and Accept-Language negotiation"
```

---

### Task 2: Dictionaries and the `t()` helper

**Files:**
- Create: `src/lib/i18n/dictionaries/he.ts`, `src/lib/i18n/dictionaries/ru.ts`, `src/lib/i18n/dictionaries/en.ts`, `src/lib/i18n/t.ts`
- Modify: `tests/i18n.test.ts` (append the integrity suite)

**Interfaces:**
- Consumes: `Locale`, `LOCALES` from `@/lib/i18n/locales`.
- Produces:
  ```ts
  export type Dictionary = typeof he;      // from t.ts
  export type MessageKey = keyof Dictionary;
  export function getDictionary(locale: Locale): Dictionary;
  export function format(template: string, params?: Record<string, string | number>): string;
  export function t(dict: Dictionary, key: MessageKey, params?: Record<string, string | number>): string;
  ```
  `ru.ts` and `en.ts` are each declared `const ru: Dictionary = {...}`, so TypeScript rejects a missing, extra, or misspelled key at build time.

- [ ] **Step 1: Write the Hebrew source-of-truth dictionary**

Create `src/lib/i18n/dictionaries/he.ts`. Flat dotted keys — greppable, and key-set equality is trivial to assert.

```ts
/**
 * Source of truth for every chrome string. `ru.ts` and `en.ts` are typed against
 * this object, so a key added here without a translation is a build error.
 *
 * `seed.*` are the builder's pre-filled suggestions: app-authored, but they turn
 * into creator content the moment they are accepted, so they are translated too.
 */
export const he = {
  "meta.title": "תגידי כן",
  "meta.description": "הזמנות דייט חמודות",

  "switcher.label": "שפה",

  "mascot.bear": "דובי",
  "mascot.penguin": "פינגווין",

  "dashboard.title": "ההזמנות שלי",
  "dashboard.subtitle": "כל ההזמנות שיצרתם",
  "dashboard.new": "+ הזמנה חדשה",
  "dashboard.empty.text": "עוד לא יצרתם הזמנות.",
  "dashboard.empty.cta": "יצירת ההזמנה הראשונה",
  "dashboard.status.answered": "נענתה",
  "dashboard.status.pending": "ממתינה",
  "dashboard.createdAt": "נוצרה {date}",
  "dashboard.answeredAt": "נענתה {date}",
  "dashboard.copy": "העתקת קישור",
  "dashboard.copied": "הועתק!",
  "dashboard.answers": "התשובות",
  "dashboard.close": "סגירה",
  "dashboard.open": "פתיחה",
  "dashboard.recapTitle": "מה {name} בחרה",

  "builder.title": "הזמנה חדשה",
  "builder.back": "← לרשימה",
  "builder.name.label": "למי ההזמנה?",
  "builder.name.placeholder": "השם שלה",
  "builder.mascot.label": "איזה קמע ילווה אותה?",
  "builder.gate.label": "שאלת הפתיחה",
  "builder.questions.title": "שאלות הלוגיסטיקה",
  "builder.question.placeholder": "מה נשאל?",
  "builder.option.placeholder": "תשובה {n}",
  "builder.moveUp": "הזזה למעלה",
  "builder.moveDown": "הזזה למטה",
  "builder.removeQuestion": "מחיקת שאלה",
  "builder.removeOption": "מחיקת תשובה",
  "builder.addOption": "+ עוד תשובה",
  "builder.addQuestion": "+ הוספת שאלה",
  "builder.submit": "יצירת קישור",
  "builder.submitting": "רגע...",
  "builder.done.title": "ההזמנה מוכנה!",
  "builder.done.subtitle": "שלחו לה את הקישור הזה:",
  "builder.done.copy": "העתקה",
  "builder.done.copied": "הועתק!",
  "builder.done.note": "אפשר לשלוח את הקישור בכל אפליקציה. היא תוכל לענות עליו פעם אחת.",
  "builder.done.preview": "תצוגה מקדימה",
  "builder.done.back": "חזרה לרשימה",

  "invite.gate.intro": "היי {name}, יש לי משהו לשאול...",
  "invite.yes": "כן",
  "invite.no": "לא",
  "invite.retry": "ניסיון נוסף",
  "invite.finale.title": "יש!! 🎉",
  "invite.finale.text": "תודה {name}, אני כבר מתרגש. נתראה!",
  "invite.missing.title": "אופס, ההזמנה לא נמצאה",
  "invite.missing.text": "כנראה שהקישור לא מדויק. אולי כדאי לבקש אותו שוב?",
  "invite.answered.title": "כבר סגרנו הכול 💕",
  "invite.answered.text": "{name}, אלה התשובות ששלחת — נתראה בדייט!",

  "recap.title": "מה שסגרנו",
  "recap.empty": "אין עדיין תשובות",

  "plea.1": "בבקשה 🥺",
  "plea.2": "בבקשה בבקשה 😢",
  "plea.3": "תשקלי את זה שוב? 🥹",
  "plea.4": "אני כבר נשבר 😭",
  "plea.5": "אנא ממך 🥺💔",

  "error.name.required": "צריך למלא שם",
  "error.name.tooLong": "שם ארוך מדי (עד {max} תווים)",
  "error.mascot.required": "צריך לבחור קמע",
  "error.gate.required": "צריך שאלת פתיחה",
  "error.text.tooLong": "שאלה ארוכה מדי (עד {max} תווים)",
  "error.questions.empty": "צריך לפחות שאלה אחת",
  "error.question.required": "צריך למלא את השאלה",
  "error.options.count": "צריך בין {min} ל-{max} תשובות",
  "error.options.empty": "יש תשובה ריקה",
  "error.options.tooLong": "תשובה ארוכה מדי (עד {max} תווים)",
  "error.options.duplicate": "יש תשובות זהות",

  "api.badRequest": "בקשה לא תקינה",
  "api.notFound": "ההזמנה לא נמצאה",
  "api.alreadyAnswered": "ההזמנה כבר נענתה",
  "api.missingAnswers": "חסרות תשובות",
  "api.invalidAnswer": "תשובה לא תקינה",
  "api.saveFailed": "לא הצלחנו לשמור, ננסה שוב?",
  "api.createFailed": "משהו נתקע, ננסה שוב?",

  "seed.gate": "תרצי לצאת איתי לדייט?",
  "seed.food.q": "מה בא לך לאכול?",
  "seed.food.a1": "סושי",
  "seed.food.a2": "פיצה",
  "seed.food.a3": "המבורגר",
  "seed.place.q": "לאן נלך אחר כך?",
  "seed.place.a1": "טיילת",
  "seed.place.a2": "בית קפה",
  "seed.place.a3": "קולנוע",
  "seed.when.q": "מתי מסתדר לך?",
  "seed.when.a1": "יום חמישי",
  "seed.when.a2": "שישי בערב",
  "seed.when.a3": "מוצאי שבת",
} as const;
```

Two Hebrew strings intentionally changed while moving:
- `dashboard.subtitle` was `"כל ההזמנות שיצרתם במחשב הזה"` — "on this computer" stopped being true when the app moved to Postgres and Vercel.
- `builder.done.note` was `"הקישור עובד רק כל עוד השרת המקומי רץ במחשב שלכם."` — same reason; it described the deleted SQLite/local-only setup.

- [ ] **Step 2: Write the Russian dictionary**

Create `src/lib/i18n/dictionaries/ru.ts`:

```ts
import type { Dictionary } from "../t";

export const ru: Dictionary = {
  "meta.title": "Скажи да",
  "meta.description": "Милые приглашения на свидание",

  "switcher.label": "Язык",

  "mascot.bear": "Мишка",
  "mascot.penguin": "Пингвин",

  "dashboard.title": "Мои приглашения",
  "dashboard.subtitle": "Все приглашения, которые вы создали",
  "dashboard.new": "+ Новое приглашение",
  "dashboard.empty.text": "Вы ещё не создали ни одного приглашения.",
  "dashboard.empty.cta": "Создать первое приглашение",
  "dashboard.status.answered": "Ответила",
  "dashboard.status.pending": "Ждём ответа",
  "dashboard.createdAt": "Создано {date}",
  "dashboard.answeredAt": "ответ {date}",
  "dashboard.copy": "Копировать ссылку",
  "dashboard.copied": "Скопировано!",
  "dashboard.answers": "Ответы",
  "dashboard.close": "Закрыть",
  "dashboard.open": "Открыть",
  "dashboard.recapTitle": "Что выбрала {name}",

  "builder.title": "Новое приглашение",
  "builder.back": "← К списку",
  "builder.name.label": "Для кого приглашение?",
  "builder.name.placeholder": "Её имя",
  "builder.mascot.label": "Какой талисман её проводит?",
  "builder.gate.label": "Главный вопрос",
  "builder.questions.title": "Вопросы о деталях",
  "builder.question.placeholder": "О чём спросим?",
  "builder.option.placeholder": "Ответ {n}",
  "builder.moveUp": "Переместить вверх",
  "builder.moveDown": "Переместить вниз",
  "builder.removeQuestion": "Удалить вопрос",
  "builder.removeOption": "Удалить ответ",
  "builder.addOption": "+ Ещё ответ",
  "builder.addQuestion": "+ Добавить вопрос",
  "builder.submit": "Создать ссылку",
  "builder.submitting": "Секунду...",
  "builder.done.title": "Приглашение готово!",
  "builder.done.subtitle": "Отправьте ей эту ссылку:",
  "builder.done.copy": "Копировать",
  "builder.done.copied": "Скопировано!",
  "builder.done.note": "Ссылку можно отправить в любом мессенджере. Ответить на неё можно только один раз.",
  "builder.done.preview": "Предпросмотр",
  "builder.done.back": "Вернуться к списку",

  "invite.gate.intro": "Привет, {name}, я хочу кое-что спросить...",
  "invite.yes": "Да",
  "invite.no": "Нет",
  "invite.retry": "Ещё раз",
  "invite.finale.title": "Ура!! 🎉",
  "invite.finale.text": "Спасибо, {name}, я уже волнуюсь. До встречи!",
  "invite.missing.title": "Ой, приглашение не найдено",
  "invite.missing.text": "Похоже, ссылка неверная. Может, попросить её заново?",
  "invite.answered.title": "Всё уже решено 💕",
  "invite.answered.text": "{name}, вот твои ответы — до встречи на свидании!",

  "recap.title": "Что мы решили",
  "recap.empty": "Ответов пока нет",

  "plea.1": "пожалуйста 🥺",
  "plea.2": "пожалуйста, пожалуйста 😢",
  "plea.3": "может, передумаешь? 🥹",
  "plea.4": "я уже не выдерживаю 😭",
  "plea.5": "умоляю тебя 🥺💔",

  "error.name.required": "Нужно указать имя",
  "error.name.tooLong": "Имя слишком длинное (до {max} символов)",
  "error.mascot.required": "Нужно выбрать талисман",
  "error.gate.required": "Нужен главный вопрос",
  "error.text.tooLong": "Вопрос слишком длинный (до {max} символов)",
  "error.questions.empty": "Нужен хотя бы один вопрос",
  "error.question.required": "Нужно заполнить вопрос",
  "error.options.count": "Нужно от {min} до {max} ответов",
  "error.options.empty": "Один из ответов пуст",
  "error.options.tooLong": "Ответ слишком длинный (до {max} символов)",
  "error.options.duplicate": "Есть одинаковые ответы",

  "api.badRequest": "Некорректный запрос",
  "api.notFound": "Приглашение не найдено",
  "api.alreadyAnswered": "На это приглашение уже ответили",
  "api.missingAnswers": "Не хватает ответов",
  "api.invalidAnswer": "Некорректный ответ",
  "api.saveFailed": "Не получилось сохранить, попробуем снова?",
  "api.createFailed": "Что-то заело, попробуем снова?",

  "seed.gate": "Пойдёшь со мной на свидание?",
  "seed.food.q": "Что хочешь поесть?",
  "seed.food.a1": "Суши",
  "seed.food.a2": "Пицца",
  "seed.food.a3": "Бургер",
  "seed.place.q": "Куда пойдём потом?",
  "seed.place.a1": "На набережную",
  "seed.place.a2": "В кафе",
  "seed.place.a3": "В кино",
  "seed.when.q": "Когда тебе удобно?",
  "seed.when.a1": "В четверг",
  "seed.when.a2": "В пятницу вечером",
  "seed.when.a3": "В субботу вечером",
};
```

- [ ] **Step 3: Write the English dictionary**

Create `src/lib/i18n/dictionaries/en.ts`:

```ts
import type { Dictionary } from "../t";

export const en: Dictionary = {
  "meta.title": "Say Yes",
  "meta.description": "Cute date invitations",

  "switcher.label": "Language",

  "mascot.bear": "Bear",
  "mascot.penguin": "Penguin",

  "dashboard.title": "My invitations",
  "dashboard.subtitle": "Every invitation you've created",
  "dashboard.new": "+ New invitation",
  "dashboard.empty.text": "You haven't created any invitations yet.",
  "dashboard.empty.cta": "Create the first one",
  "dashboard.status.answered": "Answered",
  "dashboard.status.pending": "Waiting",
  "dashboard.createdAt": "Created {date}",
  "dashboard.answeredAt": "answered {date}",
  "dashboard.copy": "Copy link",
  "dashboard.copied": "Copied!",
  "dashboard.answers": "Answers",
  "dashboard.close": "Close",
  "dashboard.open": "Open",
  "dashboard.recapTitle": "What {name} picked",

  "builder.title": "New invitation",
  "builder.back": "← Back to list",
  "builder.name.label": "Who's it for?",
  "builder.name.placeholder": "Her name",
  "builder.mascot.label": "Which mascot goes with her?",
  "builder.gate.label": "The opening question",
  "builder.questions.title": "The logistics questions",
  "builder.question.placeholder": "What shall we ask?",
  "builder.option.placeholder": "Answer {n}",
  "builder.moveUp": "Move up",
  "builder.moveDown": "Move down",
  "builder.removeQuestion": "Delete question",
  "builder.removeOption": "Delete answer",
  "builder.addOption": "+ One more answer",
  "builder.addQuestion": "+ Add question",
  "builder.submit": "Create the link",
  "builder.submitting": "One sec...",
  "builder.done.title": "The invitation is ready!",
  "builder.done.subtitle": "Send her this link:",
  "builder.done.copy": "Copy",
  "builder.done.copied": "Copied!",
  "builder.done.note": "Send the link from any app. She can answer it once.",
  "builder.done.preview": "Preview",
  "builder.done.back": "Back to the list",

  "invite.gate.intro": "Hey {name}, I've got something to ask...",
  "invite.yes": "Yes",
  "invite.no": "No",
  "invite.retry": "Try again",
  "invite.finale.title": "Yes!! 🎉",
  "invite.finale.text": "Thank you {name}, I'm already excited. See you!",
  "invite.missing.title": "Oops, that invitation isn't here",
  "invite.missing.text": "The link looks off. Maybe ask for it again?",
  "invite.answered.title": "It's all settled 💕",
  "invite.answered.text": "{name}, these are the answers you sent — see you on the date!",

  "recap.title": "What we settled on",
  "recap.empty": "No answers yet",

  "plea.1": "please 🥺",
  "plea.2": "please please 😢",
  "plea.3": "would you reconsider? 🥹",
  "plea.4": "I'm breaking here 😭",
  "plea.5": "I'm begging you 🥺💔",

  "error.name.required": "A name is required",
  "error.name.tooLong": "Name is too long (up to {max} characters)",
  "error.mascot.required": "Pick a mascot",
  "error.gate.required": "An opening question is required",
  "error.text.tooLong": "Question is too long (up to {max} characters)",
  "error.questions.empty": "At least one question is required",
  "error.question.required": "Fill in the question",
  "error.options.count": "Between {min} and {max} answers are needed",
  "error.options.empty": "One of the answers is empty",
  "error.options.tooLong": "Answer is too long (up to {max} characters)",
  "error.options.duplicate": "Two answers are identical",

  "api.badRequest": "Bad request",
  "api.notFound": "Invitation not found",
  "api.alreadyAnswered": "This invitation was already answered",
  "api.missingAnswers": "Some answers are missing",
  "api.invalidAnswer": "Invalid answer",
  "api.saveFailed": "We couldn't save that — try again?",
  "api.createFailed": "Something jammed — try again?",

  "seed.gate": "Will you go on a date with me?",
  "seed.food.q": "What do you feel like eating?",
  "seed.food.a1": "Sushi",
  "seed.food.a2": "Pizza",
  "seed.food.a3": "Burgers",
  "seed.place.q": "Where to afterwards?",
  "seed.place.a1": "The promenade",
  "seed.place.a2": "A café",
  "seed.place.a3": "The cinema",
  "seed.when.q": "When works for you?",
  "seed.when.a1": "Thursday",
  "seed.when.a2": "Friday evening",
  "seed.when.a3": "Saturday night",
};
```

- [ ] **Step 4: Write `t.ts`**

Create `src/lib/i18n/t.ts`:

```ts
import type { Locale } from "./locales";
import { he } from "./dictionaries/he";
import { ru } from "./dictionaries/ru";
import { en } from "./dictionaries/en";

/**
 * Hebrew is the source of truth: `ru` and `en` are declared `: Dictionary`, so
 * a key added here without a translation fails the type check rather than
 * falling back at runtime.
 */
export type Dictionary = Record<keyof typeof he, string>;
export type MessageKey = keyof Dictionary;

const DICTIONARIES: Record<Locale, Dictionary> = { he, ru, en };

export function getDictionary(locale: Locale): Dictionary {
  return DICTIONARIES[locale];
}

/** Replaces every `{name}` with `params.name`. Unknown placeholders stay put. */
export function format(
  template: string,
  params?: Record<string, string | number>
): string {
  if (!params) return template;
  return template.replace(/\{(\w+)\}/g, (whole, key: string) =>
    key in params ? String(params[key]) : whole
  );
}

export function t(
  dict: Dictionary,
  key: MessageKey,
  params?: Record<string, string | number>
): string {
  return format(dict[key], params);
}
```

Note the deliberate `Record<keyof typeof he, string>` rather than `typeof he`:
`he` is `as const`, so `typeof he` would pin each value to its exact literal and
every translation would be a type error.

- [ ] **Step 5: Append the integrity tests**

Add to `tests/i18n.test.ts`:

```ts
import { he } from "@/lib/i18n/dictionaries/he";
import { ru } from "@/lib/i18n/dictionaries/ru";
import { en } from "@/lib/i18n/dictionaries/en";
import { format, getDictionary, t } from "@/lib/i18n/t";
import { LOCALES } from "@/lib/i18n/locales";

const placeholders = (value: string) =>
  [...value.matchAll(/\{(\w+)\}/g)].map((match) => match[1]).sort();

describe("dictionary integrity", () => {
  const others = { ru, en } as const;

  it("every locale has a dictionary", () => {
    for (const locale of LOCALES) {
      expect(Object.keys(getDictionary(locale)).length).toBeGreaterThan(0);
    }
  });

  it("translations have exactly the Hebrew key set", () => {
    const expected = Object.keys(he).sort();
    for (const [name, dict] of Object.entries(others)) {
      expect(Object.keys(dict).sort(), name).toEqual(expected);
    }
  });

  it("has no empty strings anywhere", () => {
    for (const [name, dict] of Object.entries({ he, ...others })) {
      for (const [key, value] of Object.entries(dict)) {
        expect(value.trim().length, `${name}:${key}`).toBeGreaterThan(0);
      }
    }
  });

  it("keeps the same placeholders in every translation", () => {
    for (const [name, dict] of Object.entries(others)) {
      for (const key of Object.keys(he) as (keyof typeof he)[]) {
        expect(placeholders(dict[key]), `${name}:${key}`).toEqual(
          placeholders(he[key])
        );
      }
    }
  });
});

describe("format", () => {
  it("substitutes named placeholders", () => {
    expect(format("Hey {name}!", { name: "Maya" })).toBe("Hey Maya!");
  });

  it("substitutes numbers and repeats", () => {
    expect(format("{a} and {a} and {b}", { a: 1, b: 2 })).toBe("1 and 1 and 2");
  });

  it("leaves unknown placeholders alone rather than printing undefined", () => {
    expect(format("Hey {name}!", { other: "x" })).toBe("Hey {name}!");
    expect(format("Hey {name}!")).toBe("Hey {name}!");
  });
});

describe("t", () => {
  it("resolves a key in the requested locale", () => {
    expect(t(getDictionary("en"), "invite.yes")).toBe("Yes");
    expect(t(getDictionary("ru"), "invite.yes")).toBe("Да");
    expect(t(getDictionary("he"), "invite.yes")).toBe("כן");
  });

  it("interpolates", () => {
    expect(t(getDictionary("en"), "invite.gate.intro", { name: "Maya" })).toBe(
      "Hey Maya, I've got something to ask..."
    );
  });
});
```

- [ ] **Step 6: Run the tests**

Run: `npm test -- tests/i18n.test.ts`
Expected: PASS. A key-set or placeholder mismatch means a typo in one of the three tables — fix the table, not the test.

- [ ] **Step 7: Type check**

Run: `npx tsc --noEmit`
Expected: no errors. An error inside `ru.ts`/`en.ts` means a key is missing or misspelled there.

- [ ] **Step 8: Prepared commit (deferred)**

```bash
git add src/lib/i18n tests/i18n.test.ts
git commit -m "feat(i18n): add he/ru/en dictionaries and the t() helper"
```

---

### Task 3: Validation returns error codes

**Files:**
- Modify: `src/lib/types.ts`, `src/lib/validation.ts`
- Test: `tests/validation.test.ts`

**Interfaces:**
- Consumes: `MessageKey` from `@/lib/i18n/t`; the length constants from `@/lib/defaults`.
- Produces:
  ```ts
  export type FieldError = { code: MessageKey; params?: Record<string, string | number> };
  export type QuestionFieldErrors = { text?: FieldError; options?: FieldError };
  export type DraftErrors = {
    recipientName?: FieldError;
    mascot?: FieldError;
    gateQuestion?: FieldError;
    questions?: FieldError;
    byQuestion: Record<string, QuestionFieldErrors>;
  };
  export type Draft = { recipientName: string; mascot: MascotKind | null; gateQuestion: string; questions: DraftQuestion[]; locale: Locale };
  export function validateDraft(draft: Draft): { valid: boolean; errors: DraftErrors };
  ```
  `validateDraft` does **not** validate `locale` — the route narrows it with `isLocale()` before constructing the `Draft`.

- [ ] **Step 1: Read the existing validation tests**

Run: `cat tests/validation.test.ts`
Every assertion comparing an error to a Hebrew sentence has to become a code comparison. Keep the same cases — only the expected shape changes.

- [ ] **Step 2: Rewrite the assertions to expect codes**

Replace each Hebrew-sentence assertion with its code, using this mapping:

| Old Hebrew sentence | New expectation |
|---|---|
| `"צריך למלא שם"` | `{ code: "error.name.required" }` |
| `` `שם ארוך מדי (עד ${MAX_NAME_LENGTH} תווים)` `` | `{ code: "error.name.tooLong", params: { max: MAX_NAME_LENGTH } }` |
| `"צריך לבחור קמע"` | `{ code: "error.mascot.required" }` |
| `"צריך שאלת פתיחה"` | `{ code: "error.gate.required" }` |
| `` `שאלה ארוכה מדי (עד ${MAX_TEXT_LENGTH} תווים)` `` | `{ code: "error.text.tooLong", params: { max: MAX_TEXT_LENGTH } }` |
| `"צריך לפחות שאלה אחת"` | `{ code: "error.questions.empty" }` |
| `"צריך למלא את השאלה"` | `{ code: "error.question.required" }` |
| `` `צריך בין ${MIN_OPTIONS} ל-${MAX_OPTIONS} תשובות` `` | `{ code: "error.options.count", params: { min: MIN_OPTIONS, max: MAX_OPTIONS } }` |
| `"יש תשובה ריקה"` | `{ code: "error.options.empty" }` |
| `` `תשובה ארוכה מדי (עד ${MAX_OPTION_LENGTH} תווים)` `` | `{ code: "error.options.tooLong", params: { max: MAX_OPTION_LENGTH } }` |
| `"יש תשובות זהות"` | `{ code: "error.options.duplicate" }` |

Every `Draft` literal in the test file also needs the new required field — add `locale: "he"`.

Then append these cases:

```ts
it("carries the limit as a param rather than baking it into a sentence", () => {
  const { errors } = validateDraft({
    recipientName: "x".repeat(MAX_NAME_LENGTH + 1),
    mascot: "BEAR",
    gateQuestion: "?",
    questions: [{ id: "q1", text: "t", options: ["a", "b"] }],
    locale: "he",
  });
  expect(errors.recipientName).toEqual({
    code: "error.name.tooLong",
    params: { max: MAX_NAME_LENGTH },
  });
});

it("uses the same too-long code for the gate and for a question", () => {
  const long = "x".repeat(MAX_TEXT_LENGTH + 1);
  const { errors } = validateDraft({
    recipientName: "Maya",
    mascot: "BEAR",
    gateQuestion: long,
    questions: [{ id: "q1", text: long, options: ["a", "b"] }],
    locale: "he",
  });
  expect(errors.gateQuestion?.code).toBe("error.text.tooLong");
  expect(errors.byQuestion.q1?.text?.code).toBe("error.text.tooLong");
});
```

- [ ] **Step 3: Run the tests to verify they fail**

Run: `npm test -- tests/validation.test.ts`
Expected: FAIL — received Hebrew strings, expected objects.

- [ ] **Step 4: Update the types**

In `src/lib/types.ts`:

```ts
import type { Locale } from "@/lib/i18n/locales";
import type { MessageKey } from "@/lib/i18n/t";

/**
 * Validation failures travel as codes, not sentences — `src/lib/` has no locale,
 * so the message is resolved wherever it is rendered.
 */
export type FieldError = {
  code: MessageKey;
  params?: Record<string, string | number>;
};

export type QuestionFieldErrors = {
  text?: FieldError;
  options?: FieldError;
};

export type DraftErrors = {
  recipientName?: FieldError;
  mascot?: FieldError;
  gateQuestion?: FieldError;
  /** Problem with the question list as a whole (e.g. there are none). */
  questions?: FieldError;
  byQuestion: Record<string, QuestionFieldErrors>;
};
```

Add `locale` to `Draft` and to `InviteView`:

```ts
export type Draft = {
  recipientName: string;
  mascot: MascotKind | null;
  gateQuestion: string;
  questions: DraftQuestion[];
  /** The language the creator is authoring in — stored on the invitation. */
  locale: Locale;
};

export type InviteView = {
  token: string;
  /** The invitation's own locale; the recipient's chrome follows it. */
  locale: Locale;
  recipientName: string;
  mascot: MascotKind;
  gateQuestion: string;
  questions: {
    id: string;
    text: string;
    options: { id: string; label: string }[];
  }[];
};
```

- [ ] **Step 5: Update `validation.ts`**

Replace every returned sentence with a code. The control flow does not change:

```ts
function validateQuestionOptions(options: string[]): FieldError | undefined {
  const labels = options.map((option) => option.trim());

  if (labels.length < MIN_OPTIONS || labels.length > MAX_OPTIONS) {
    return { code: "error.options.count", params: { min: MIN_OPTIONS, max: MAX_OPTIONS } };
  }
  if (labels.some((label) => label.length === 0)) {
    return { code: "error.options.empty" };
  }
  if (labels.some((label) => label.length > MAX_OPTION_LENGTH)) {
    return { code: "error.options.tooLong", params: { max: MAX_OPTION_LENGTH } };
  }
  if (new Set(labels).size !== labels.length) {
    return { code: "error.options.duplicate" };
  }
  return undefined;
}
```

and in `validateDraft`:

```ts
  const name = draft.recipientName.trim();
  if (name.length === 0) {
    errors.recipientName = { code: "error.name.required" };
  } else if (name.length > MAX_NAME_LENGTH) {
    errors.recipientName = { code: "error.name.tooLong", params: { max: MAX_NAME_LENGTH } };
  }

  if (draft.mascot === null || !MASCOTS.includes(draft.mascot)) {
    errors.mascot = { code: "error.mascot.required" };
  }

  const gate = draft.gateQuestion.trim();
  if (gate.length === 0) {
    errors.gateQuestion = { code: "error.gate.required" };
  } else if (gate.length > MAX_TEXT_LENGTH) {
    errors.gateQuestion = { code: "error.text.tooLong", params: { max: MAX_TEXT_LENGTH } };
  }

  if (draft.questions.length === 0) {
    errors.questions = { code: "error.questions.empty" };
  }
```

and inside the question loop:

```ts
    const text = question.text.trim();
    if (text.length === 0) {
      questionErrors.text = { code: "error.question.required" };
    } else if (text.length > MAX_TEXT_LENGTH) {
      questionErrors.text = { code: "error.text.tooLong", params: { max: MAX_TEXT_LENGTH } };
    }
```

Import `FieldError` alongside the existing type imports. The `valid` computation at the bottom is unchanged.

- [ ] **Step 6: Run the tests to verify they pass**

Run: `npm test -- tests/validation.test.ts`
Expected: PASS.

- [ ] **Step 7: Prepared commit (deferred)**

```bash
git add src/lib/types.ts src/lib/validation.ts tests/validation.test.ts
git commit -m "refactor(i18n): validation returns error codes instead of Hebrew sentences"
```

---

### Task 4: Dodge pleas become dictionary keys

**Files:**
- Modify: `src/lib/dodge.ts`
- Test: `tests/dodge.test.ts`

**Interfaces:**
- Consumes: `MessageKey` from `@/lib/i18n/t`.
- Produces:
  ```ts
  export const PLEA_KEYS: readonly ["plea.1", "plea.2", "plea.3", "plea.4", "plea.5"];
  export type PleaKey = (typeof PLEA_KEYS)[number];
  export function pleaKeyForCatches(catches: number): PleaKey | null;
  ```
  `PLEAS` and `pleaForCatches` are removed. Everything else in `dodge.ts` is untouched.

- [ ] **Step 1: Rewrite the plea tests**

In `tests/dodge.test.ts`, replace the `pleaForCatches` block with:

```ts
import { PLEA_KEYS, pleaKeyForCatches } from "@/lib/dodge";

describe("pleaKeyForCatches", () => {
  it("says nothing before the first catch", () => {
    expect(pleaKeyForCatches(0)).toBeNull();
    expect(pleaKeyForCatches(-1)).toBeNull();
  });

  it("escalates one step per catch", () => {
    expect(pleaKeyForCatches(1)).toBe("plea.1");
    expect(pleaKeyForCatches(2)).toBe("plea.2");
    expect(pleaKeyForCatches(5)).toBe("plea.5");
  });

  it("repeats the last plea forever — it never gives up", () => {
    expect(pleaKeyForCatches(6)).toBe(PLEA_KEYS[PLEA_KEYS.length - 1]);
    expect(pleaKeyForCatches(500)).toBe(PLEA_KEYS[PLEA_KEYS.length - 1]);
  });
});
```

Leave the `nextDodgeOffset`, `nextFleeOffset`, and `scaleForCatches` suites exactly as they are.

- [ ] **Step 2: Run the tests to verify they fail**

Run: `npm test -- tests/dodge.test.ts`
Expected: FAIL — `pleaKeyForCatches is not a function`.

- [ ] **Step 3: Update `dodge.ts`**

Replace the `PLEAS` / `pleaForCatches` block at the bottom of the file:

```ts
/**
 * Escalating pleas shown once she starts landing clicks on the dodge button.
 * Keys, not sentences — the strings live in the dictionaries so all three
 * languages escalate the same way.
 */
export const PLEA_KEYS = ["plea.1", "plea.2", "plea.3", "plea.4", "plea.5"] as const;

export type PleaKey = (typeof PLEA_KEYS)[number];

/** The plea key for `catches` clicks — nothing before the first, clamped at the last. */
export function pleaKeyForCatches(catches: number): PleaKey | null {
  if (catches < 1) return null;
  return PLEA_KEYS[Math.min(catches, PLEA_KEYS.length) - 1];
}
```

Also update the two Hebrew-quoting comments so the file no longer claims a single language: the module docstring's `"לא" button` becomes `dodge ("no") button`.

- [ ] **Step 4: Run the tests to verify they pass**

Run: `npm test -- tests/dodge.test.ts`
Expected: PASS.

- [ ] **Step 5: Update `DodgeButton` to take resolved strings**

`src/components/DodgeButton.tsx` currently calls `pleaForCatches`. Change the props and the lookup — the component receives already-translated pleas so it needs no dictionary of its own:

```ts
export function DodgeButton({
  label,
  pleas,
  onDodge,
}: {
  label: string;
  /** Already-translated pleas, longest-suffering last. */
  pleas: readonly string[];
  onDodge?: () => void;
}) {
```

Replace the `pleaForCatches` import with `pleaKeyForCatches`, and the call site:

```ts
  const pleaKey = pleaKeyForCatches(catches);
  const plea = pleaKey ? pleas[PLEA_KEYS.indexOf(pleaKey)] : null;
```

Import `PLEA_KEYS` too. The `AnimatePresence` block below keys off `plea` and needs no change. Update the module docstring's `"לא"` to `"no"`.

- [ ] **Step 6: Prepared commit (deferred)**

```bash
git add src/lib/dodge.ts src/components/DodgeButton.tsx tests/dodge.test.ts
git commit -m "refactor(i18n): dodge pleas become dictionary keys"
```

---

### Task 5: Locale-aware builder seeds

**Files:**
- Modify: `src/lib/defaults.ts`

**Interfaces:**
- Consumes: `Locale` from `@/lib/i18n/locales`; `getDictionary`, `t` from `@/lib/i18n/t`.
- Produces:
  ```ts
  export const MIN_OPTIONS = 2;
  export const MAX_OPTIONS = 4;
  export const MAX_NAME_LENGTH = 40;
  export const MAX_TEXT_LENGTH = 120;
  export const MAX_OPTION_LENGTH = 40;
  export function defaultGateQuestion(locale: Locale): string;
  export function defaultQuestions(locale: Locale): DraftQuestion[];
  export function emptyQuestion(id: string): DraftQuestion;
  ```
  `DEFAULT_GATE_QUESTION` is removed. The constants keep their exact values.

- [ ] **Step 1: Rewrite `defaults.ts`**

```ts
import type { Locale } from "./i18n/locales";
import { getDictionary, t } from "./i18n/t";
import type { DraftQuestion } from "./types";

export const MIN_OPTIONS = 2;
export const MAX_OPTIONS = 4;

export const MAX_NAME_LENGTH = 40;
export const MAX_TEXT_LENGTH = 120;
export const MAX_OPTION_LENGTH = 40;

/** The pre-filled gate question, in the creator's language. */
export function defaultGateQuestion(locale: Locale): string {
  return t(getDictionary(locale), "seed.gate");
}

/**
 * The three logistics questions pre-loaded into a fresh builder, in the
 * creator's language. These are seeds, not chrome — the creator edits them and
 * whatever they end up as is stored verbatim and never translated again.
 */
export function defaultQuestions(locale: Locale): DraftQuestion[] {
  const dict = getDictionary(locale);
  return [
    {
      id: "default-food",
      text: t(dict, "seed.food.q"),
      options: [t(dict, "seed.food.a1"), t(dict, "seed.food.a2"), t(dict, "seed.food.a3")],
    },
    {
      id: "default-place",
      text: t(dict, "seed.place.q"),
      options: [t(dict, "seed.place.a1"), t(dict, "seed.place.a2"), t(dict, "seed.place.a3")],
    },
    {
      id: "default-when",
      text: t(dict, "seed.when.q"),
      options: [t(dict, "seed.when.a1"), t(dict, "seed.when.a2"), t(dict, "seed.when.a3")],
    },
  ];
}

/** A blank question for the "add question" button. */
export function emptyQuestion(id: string): DraftQuestion {
  return { id, text: "", options: ["", ""] };
}
```

- [ ] **Step 2: Add a seed test**

Append to `tests/i18n.test.ts`:

```ts
import { MAX_OPTIONS, MIN_OPTIONS, defaultGateQuestion, defaultQuestions } from "@/lib/defaults";
import { validateDraft } from "@/lib/validation";

describe("builder seeds", () => {
  it("seeds a valid draft in every locale", () => {
    for (const locale of LOCALES) {
      const { valid } = validateDraft({
        recipientName: "Maya",
        mascot: "BEAR",
        gateQuestion: defaultGateQuestion(locale),
        questions: defaultQuestions(locale),
        locale,
      });
      expect(valid, locale).toBe(true);
    }
  });

  it("seeds three questions with legal option counts", () => {
    for (const locale of LOCALES) {
      const questions = defaultQuestions(locale);
      expect(questions).toHaveLength(3);
      for (const question of questions) {
        expect(question.options.length).toBeGreaterThanOrEqual(MIN_OPTIONS);
        expect(question.options.length).toBeLessThanOrEqual(MAX_OPTIONS);
      }
    }
  });

  it("seeds different text per locale", () => {
    expect(defaultGateQuestion("he")).not.toBe(defaultGateQuestion("en"));
    expect(defaultGateQuestion("ru")).not.toBe(defaultGateQuestion("en"));
  });
});
```

- [ ] **Step 3: Run the full unit suite**

Run: `npm test`
Expected: PASS. `BuilderForm.tsx` will not type check yet — that is Task 10.

- [ ] **Step 4: Prepared commit (deferred)**

```bash
git add src/lib/defaults.ts tests/i18n.test.ts
git commit -m "feat(i18n): seed the builder in the creator's language"
```

---

### Task 6: Persist the invitation's locale; API returns codes

**Files:**
- Modify: `prisma/schema.prisma`, `src/app/api/invitations/route.ts`, `src/app/api/invitations/[token]/answers/route.ts`

**Interfaces:**
- Consumes: `isLocale`, `DEFAULT_LOCALE` from `@/lib/i18n/locales`; `validateDraft`.
- Produces: `POST /api/invitations` accepts `locale` in the body and responds `201 { id }`. Error bodies change shape:
  - `400 { code: "api.badRequest" }` for an unparseable body.
  - `400 { errors: DraftErrors }` for a failed `validateDraft` (unchanged key, new value shape).
  - `404 { code: "api.notFound" }`, `409 { code: "api.alreadyAnswered" }`, `400 { code: "api.missingAnswers" | "api.invalidAnswer" }` from the answers route.
  Clients read `code` and translate it locally, because a route handler has no reliable locale.

- [ ] **Step 1: Add the schema field**

In `prisma/schema.prisma`, inside `model Invitation`, after `gateQuestion`:

```prisma
  /// The language the creator authored in. The recipient's chrome follows it.
  locale        String     @default("he")
```

- [ ] **Step 2: Push the schema**

Run: `npm run db:push`
Expected: succeeds and reports the added column. Existing rows get `he`, which is correct — they were authored in Hebrew.

If `DATABASE_URL` is unset the command fails with the readable message from `src/lib/prisma.ts`; set it from `.env.example` first.

- [ ] **Step 3: Regenerate the client**

Run: `npx prisma generate`
Expected: succeeds. `invitation.locale` is now typed.

- [ ] **Step 4: Accept and store the locale in the create route**

In `src/app/api/invitations/route.ts`, add the import and extend `toDraft`:

```ts
import { DEFAULT_LOCALE, isLocale } from "@/lib/i18n/locales";
```

```ts
  return {
    recipientName: raw.recipientName,
    mascot: (raw.mascot as MascotKind | null) ?? null,
    gateQuestion: raw.gateQuestion,
    questions,
    // An absent or unknown locale is not worth a 400 — fall back to the default.
    locale: isLocale(raw.locale) ? raw.locale : DEFAULT_LOCALE,
  };
```

Swap the two Hebrew `message` bodies for codes:

```ts
    return NextResponse.json({ code: "api.badRequest" }, { status: 400 });
```

and persist the locale in the `create` call, right after `gateQuestion`:

```ts
      locale: draft.locale,
```

- [ ] **Step 5: Swap the answers route messages for codes**

In `src/app/api/invitations/[token]/answers/route.ts`, replace each response body:

| Old | New |
|---|---|
| `{ message: "בקשה לא תקינה" }`, `400` (both occurrences) | `{ code: "api.badRequest" }`, `400` |
| `{ message: "ההזמנה לא נמצאה" }`, `404` | `{ code: "api.notFound" }`, `404` |
| `{ message: "ההזמנה כבר נענתה" }`, `409` | `{ code: "api.alreadyAnswered" }`, `409` |
| `{ message: "חסרות תשובות" }`, `400` | `{ code: "api.missingAnswers" }`, `400` |
| `{ message: "תשובה לא תקינה" }`, `400` (both occurrences) | `{ code: "api.invalidAnswer" }`, `400` |

The status codes and all validation logic stay exactly as they are.

- [ ] **Step 6: Type check**

Run: `npx tsc --noEmit`
Expected: errors only in `BuilderForm.tsx` / `DashboardList.tsx` / `InviteFlow.tsx` / the page files — those are Tasks 7–11. No errors in `src/lib/` or `src/app/api/`.

- [ ] **Step 7: Prepared commit (deferred)**

```bash
git add prisma/schema.prisma src/app/api
git commit -m "feat(i18n): store the invitation locale and return API error codes"
```

---

### Task 7: Locale routing — middleware, `[locale]` root layout, Rubik

**Files:**
- Create: `src/middleware.ts`, `src/app/[locale]/layout.tsx`
- Move: `src/app/page.tsx` → `src/app/[locale]/page.tsx`; `src/app/new/page.tsx` → `src/app/[locale]/new/page.tsx`; `src/app/invite/[token]/page.tsx` → `src/app/[locale]/invite/[token]/page.tsx`
- Delete: `src/app/layout.tsx`
- Modify: `src/app/globals.css`

**Interfaces:**
- Consumes: everything from Tasks 1–2.
- Produces: every page receives `params: Promise<{ locale: string }>`. `src/app/[locale]/layout.tsx` is the app's root layout and renders `<html>` / `<body>`.

- [ ] **Step 1: Move the three page files**

```bash
mkdir -p "src/app/[locale]/new" "src/app/[locale]/invite/[token]"
git mv src/app/page.tsx "src/app/[locale]/page.tsx"
git mv src/app/new/page.tsx "src/app/[locale]/new/page.tsx"
git mv "src/app/invite/[token]/page.tsx" "src/app/[locale]/invite/[token]/page.tsx"
git rm src/app/layout.tsx
```

`src/app/icon.svg`, `src/app/globals.css`, and `src/app/api/` stay put. Confirm `src/app/new` and `src/app/invite` are now empty and remove the empty directories.

- [ ] **Step 2: Write the new root layout**

Create `src/app/[locale]/layout.tsx`. This is now the root layout — it owns `<html>` and `<body>`, and it is where `lang`/`dir` come from:

```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { Rubik } from "next/font/google";
import { DIR, LOCALES, isLocale } from "@/lib/i18n/locales";
import { getDictionary, t } from "@/lib/i18n/t";
import "../globals.css";

// Rubik covers all three scripts; Varela Round had no Cyrillic.
const rubik = Rubik({
  subsets: ["hebrew", "latin", "cyrillic"],
  weight: ["400", "500", "700"],
  display: "swap",
  variable: "--font-app",
});

export function generateStaticParams() {
  return LOCALES.map((locale) => ({ locale }));
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  if (!isLocale(locale)) return {};
  const dict = getDictionary(locale);
  return { title: t(dict, "meta.title"), description: t(dict, "meta.description") };
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  // The middleware only ever emits known locales; a hand-typed /de is a 404.
  if (!isLocale(locale)) notFound();

  return (
    <html lang={locale} dir={DIR[locale]} className={rubik.variable}>
      <body className="min-h-screen bg-blush antialiased">{children}</body>
    </html>
  );
}
```

- [ ] **Step 3: Rename the font variable in `globals.css`**

Change line 9 of `src/app/globals.css`:

```css
  --font-cute: var(--font-app), "Heebo", "Segoe UI", Arial, sans-serif;
```

(`"Rubik"` drops out of the fallback list because it is now the loaded font.)

- [ ] **Step 4: Write the middleware**

Create `src/middleware.ts`:

```ts
import { NextResponse, type NextRequest } from "next/server";
import {
  LOCALE_COOKIE,
  LOCALE_COOKIE_MAX_AGE,
  localeFromPath,
  resolveLocale,
} from "@/lib/i18n/locales";

/**
 * Puts a locale prefix on every page request. Detection itself lives in
 * `resolveLocale` — this only wires the request to it, so the rules stay unit
 * testable without a server.
 */
export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;
  const cookie = request.cookies.get(LOCALE_COOKIE)?.value ?? null;

  if (localeFromPath(pathname).locale) return NextResponse.next();

  const locale = resolveLocale({
    pathname,
    cookie,
    acceptLanguage: request.headers.get("accept-language"),
  });

  const url = request.nextUrl.clone();
  url.pathname = pathname === "/" ? `/${locale}` : `/${locale}${pathname}`;

  const response = NextResponse.redirect(url, 307);
  // Remember a header-negotiated choice so the negotiation runs once, not per nav.
  if (!cookie) {
    response.cookies.set(LOCALE_COOKIE, locale, {
      path: "/",
      maxAge: LOCALE_COOKIE_MAX_AGE,
      sameSite: "lax",
    });
  }
  return response;
}

export const config = {
  // API routes must not be redirected, and static assets have no locale.
  matcher: ["/((?!api|_next/static|_next/image|.*\\.).*)"],
};
```

- [ ] **Step 5: Stub the moved pages so the app compiles**

The three page files still contain Hebrew literals and do not yet accept `params`. Tasks 9–11 rewrite them. To keep this task independently verifiable, add the `params` signature to each now without touching the copy:

- `src/app/[locale]/page.tsx` — `export default async function DashboardPage({ params }: { params: Promise<{ locale: string }> })`, then `const { locale } = await params;` and `void locale;` as a placeholder.
- `src/app/[locale]/new/page.tsx` — make it `async`, same signature and placeholder.
- `src/app/[locale]/invite/[token]/page.tsx` — widen the params type to `Promise<{ locale: string; token: string }>` and destructure both.

- [ ] **Step 6: Build and check the redirect**

Run: `npm run build`
Expected: succeeds.

Run: `npm run dev`, then in another shell:

```bash
curl -si -H "Accept-Language: ru-RU,ru;q=0.9" http://localhost:3000/ | head -20
```

Expected: `HTTP/1.1 307`, `location: /ru`, and a `set-cookie: sayyes_locale=ru` header.

```bash
curl -si -H "Accept-Language: fr-FR" http://localhost:3000/new | head -12
curl -si -H "Cookie: sayyes_locale=en" -H "Accept-Language: ru-RU" http://localhost:3000/ | head -12
curl -so /dev/null -w "%{http_code}\n" http://localhost:3000/api/invitations
```

Expected: `location: /he/new` (no French, so the default); `location: /en` (cookie beats header); and the API call is **not** a 307 — it reaches the route handler.

- [ ] **Step 7: Confirm `/de` is a 404**

Visit `http://localhost:3000/de/new`.
Expected: Next's 404 page, because `isLocale` fails in the layout.

- [ ] **Step 8: Prepared commit (deferred)**

```bash
git add src/middleware.ts "src/app/[locale]" src/app/globals.css
git commit -m "feat(i18n): locale-prefixed routes, per-locale html lang/dir, Rubik font"
```

---

### Task 8: Language switcher

**Files:**
- Create: `src/components/LanguageSwitcher.tsx`

**Interfaces:**
- Consumes: `LOCALES`, `LOCALE_NAMES`, `LOCALE_COOKIE`, `LOCALE_COOKIE_MAX_AGE`, `swapLocale`, `type Locale`; `Dictionary`, `t`.
- Produces: `export function LanguageSwitcher({ locale, dict }: { locale: Locale; dict: Dictionary })`.

- [ ] **Step 1: Write the component**

Create `src/components/LanguageSwitcher.tsx`:

```tsx
"use client";

import { usePathname, useRouter } from "next/navigation";
import {
  LOCALES,
  LOCALE_COOKIE,
  LOCALE_COOKIE_MAX_AGE,
  LOCALE_NAMES,
  swapLocale,
  type Locale,
} from "@/lib/i18n/locales";
import { t, type Dictionary } from "@/lib/i18n/t";

/**
 * Three pills, each language named in its own script. Writes the cookie so the
 * choice survives a visit to a prefix-less URL, then swaps the path prefix.
 *
 * Not rendered on the invite page: the invitation's content can't follow a
 * switch, so offering one there would only produce a mixed-language card.
 */
export function LanguageSwitcher({
  locale,
  dict,
}: {
  locale: Locale;
  dict: Dictionary;
}) {
  const router = useRouter();
  const pathname = usePathname();

  function choose(next: Locale) {
    if (next === locale) return;
    document.cookie = `${LOCALE_COOKIE}=${next}; path=/; max-age=${LOCALE_COOKIE_MAX_AGE}; samesite=lax`;
    router.replace(swapLocale(pathname, next));
  }

  return (
    <div
      role="group"
      aria-label={t(dict, "switcher.label")}
      className="flex flex-wrap gap-1 rounded-2xl bg-white/70 p-1"
    >
      {LOCALES.map((option) => {
        const active = option === locale;
        return (
          <button
            key={option}
            type="button"
            lang={option}
            aria-pressed={active}
            onClick={() => choose(option)}
            className={`rounded-xl px-3 py-1.5 text-sm font-bold transition ${
              active
                ? "bg-rose-deep text-white"
                : "text-rose-ink/70 hover:bg-blush"
            }`}
          >
            {LOCALE_NAMES[option]}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: Type check**

Run: `npx tsc --noEmit`
Expected: no new errors from this file.

- [ ] **Step 3: Prepared commit (deferred)**

```bash
git add src/components/LanguageSwitcher.tsx
git commit -m "feat(i18n): add the language switcher"
```

---

### Task 9: Translate the dashboard

**Files:**
- Modify: `src/app/[locale]/page.tsx`, `src/components/DashboardList.tsx`, `src/components/Mascot.tsx`, `src/components/RecapCard.tsx`

**Interfaces:**
- Consumes: `getDictionary`, `t`, `isLocale`, `DATE_LOCALE`, `LanguageSwitcher`.
- Produces:
  ```ts
  // DashboardList.tsx
  export type DashboardItem = {
    id: string;
    recipientName: string;
    mascot: MascotKind;
    status: InvitationStatus;
    /** Already-formatted in the viewer's locale by the page. */
    createdLabel: string;
    answeredLabel: string | null;
    /** The invitation's own locale — drives the badge and the link prefix. */
    locale: Locale;
    recap: RecapItem[];
  };
  export function DashboardList({ items, dict, locale }: { items: DashboardItem[]; dict: Dictionary; locale: Locale }): JSX.Element;
  // `locale` is the viewer's, used only for the empty-state "create one" link.
  // Every per-invitation link uses that invitation's own `item.locale`.

  // Mascot.tsx — label is now required
  export function Mascot({ kind, mood, size, label, className }: { kind: MascotKind; mood?: MascotMood; size?: number; label: string; className?: string }): JSX.Element;

  // RecapCard.tsx — title is now required
  export function RecapCard({ items, title, emptyText }: { items: RecapItem[]; title: string; emptyText: string }): JSX.Element;
  ```

- [ ] **Step 1: Make `Mascot`'s label a prop**

In `src/components/Mascot.tsx`, add `label: string` to the props and replace the hardcoded `aria-label`:

```tsx
      aria-label={label}
```

Update the docstring's `hovering "לא" (blush)` to `a dodge (blush)`.

- [ ] **Step 2: Make `RecapCard`'s strings props**

Rewrite `src/components/RecapCard.tsx`'s signature — no defaults, so a caller can never forget to translate:

```tsx
export function RecapCard({
  items,
  title,
  emptyText,
}: {
  items: RecapItem[];
  title: string;
  emptyText: string;
}) {
  if (items.length === 0) {
    return <p className="text-center text-rose-ink/60">{emptyText}</p>;
  }
```

and use `{title}` in the `<h3>`. Everything else is unchanged.

- [ ] **Step 3: Rewrite the dashboard page**

`src/app/[locale]/page.tsx`:

```tsx
import Link from "next/link";
import { notFound } from "next/navigation";
import { DashboardList, type DashboardItem } from "@/components/DashboardList";
import { LanguageSwitcher } from "@/components/LanguageSwitcher";
import { Sparkles } from "@/components/Sparkles";
import { DATE_LOCALE, DEFAULT_LOCALE, isLocale } from "@/lib/i18n/locales";
import { getDictionary, t } from "@/lib/i18n/t";
import { prisma } from "@/lib/prisma";

export const dynamic = "force-dynamic";

export default async function DashboardPage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  if (!isLocale(locale)) notFound();
  const dict = getDictionary(locale);

  // Dates are chrome, so they follow the viewer's locale, not the invitation's.
  const dateFormatter = new Intl.DateTimeFormat(DATE_LOCALE[locale], {
    day: "numeric",
    month: "long",
    hour: "2-digit",
    minute: "2-digit",
  });

  const invitations = await prisma.invitation.findMany({
    orderBy: { createdAt: "desc" },
    include: {
      questions: {
        orderBy: { order: "asc" },
        include: { options: { orderBy: { order: "asc" } } },
      },
      answers: { include: { selectedOption: true } },
    },
  });

  const items: DashboardItem[] = invitations.map((invitation) => ({
    id: invitation.id,
    recipientName: invitation.recipientName,
    mascot: invitation.mascot,
    status: invitation.status,
    createdLabel: dateFormatter.format(invitation.createdAt),
    answeredLabel: invitation.answeredAt ? dateFormatter.format(invitation.answeredAt) : null,
    // A hand-edited row shouldn't crash the list.
    locale: isLocale(invitation.locale) ? invitation.locale : DEFAULT_LOCALE,
    recap: invitation.questions.map((question) => {
      const answer = invitation.answers.find((item) => item.questionId === question.id);
      return { question: question.text, answer: answer?.selectedOption.label ?? "—" };
    }),
  }));

  return (
    <>
      <Sparkles />
      <main className="relative z-10 mx-auto w-full max-w-2xl px-4 py-10">
        <div className="mb-4 flex justify-end">
          <LanguageSwitcher locale={locale} dict={dict} />
        </div>

        <div className="mb-6 flex flex-wrap items-center justify-between gap-4">
          <div>
            <h1 className="text-3xl font-bold text-rose-deep">{t(dict, "dashboard.title")}</h1>
            <p className="text-rose-ink/60">{t(dict, "dashboard.subtitle")}</p>
          </div>
          <Link
            href={`/${locale}/new`}
            className="shrink-0 rounded-2xl bg-gradient-to-b from-rose-soft to-rose-deep px-5 py-3 text-lg font-bold text-white shadow-[0_12px_30px_-14px_rgba(232,74,127,0.8)] transition hover:brightness-105"
          >
            {t(dict, "dashboard.new")}
          </Link>
        </div>

        <DashboardList items={items} dict={dict} />
      </main>
    </>
  );
}
```

Note the added `flex-wrap` on the header row — three switcher pills plus a title plus a button must not overflow 390px.

- [ ] **Step 4: Rewrite `DashboardList`**

In `src/components/DashboardList.tsx`:

Add to the imports:

```tsx
import { LOCALE_NAMES, type Locale } from "@/lib/i18n/locales";
import { t, type Dictionary } from "@/lib/i18n/t";
```

Add `locale: Locale;` to `DashboardItem` (documented as the invitation's own locale).

Give `StatusBadge` the dictionary and add a locale badge beside it:

```tsx
function StatusBadge({ status, dict }: { status: InvitationStatus; dict: Dictionary }) {
  const answered = status === "ANSWERED";
  return (
    <span
      className={`rounded-full px-3 py-1 text-sm font-bold ${
        answered ? "bg-rose-deep text-white" : "bg-blush-deep text-rose-ink/70"
      }`}
    >
      {answered ? t(dict, "dashboard.status.answered") : t(dict, "dashboard.status.pending")}
    </span>
  );
}

/** Which language this invitation was written in — the list can be mixed. */
function LocaleBadge({ locale }: { locale: Locale }) {
  return (
    <span
      lang={locale}
      title={LOCALE_NAMES[locale]}
      className="rounded-full bg-blush px-2 py-1 text-xs font-bold uppercase text-rose-deep"
    >
      {locale}
    </span>
  );
}
```

Change the component signature to
`{ items, dict, locale }: { items: DashboardItem[]; dict: Dictionary; locale: Locale }`
and pass `locale={locale}` from the page. Then:

- `copyLink(id)` → `copyLink(item: DashboardItem)`, writing
  `` `${window.location.origin}/${item.locale}/invite/${item.id}` ``, and keep
  keying `copiedId` off `item.id`.
- Empty state text: `{t(dict, "dashboard.empty.text")}`. Its CTA is the one link
  here that uses the *viewer's* locale, because there is no invitation yet:
  `href={`/${locale}/new`}`, label `{t(dict, "dashboard.empty.cta")}`.
- Empty-state mascot: `label={t(dict, "mascot.bear")}`.
- Row mascot: `label={t(dict, item.mascot === "BEAR" ? "mascot.bear" : "mascot.penguin")}`.
- The date line becomes:

```tsx
                  <p className="text-xs text-rose-ink/60 sm:text-sm">
                    {t(dict, "dashboard.createdAt", { date: item.createdLabel })}
                    {item.answeredLabel
                      ? ` · ${t(dict, "dashboard.answeredAt", { date: item.answeredLabel })}`
                      : ""}
                  </p>
```

- Badges: `<LocaleBadge locale={item.locale} /><StatusBadge status={item.status} dict={dict} />`, wrapped in `<div className="flex shrink-0 flex-wrap items-center gap-2">`.
- Copy button label: `{copiedId === item.id ? t(dict, "dashboard.copied") : t(dict, "dashboard.copy")}`.
- Expand button label: `{open ? t(dict, "dashboard.close") : t(dict, "dashboard.answers")}`.
- Open link: `href={`/${item.locale}/invite/${item.id}`}`, label `{t(dict, "dashboard.open")}`.
- Recap: `<RecapCard items={item.recap} title={t(dict, "dashboard.recapTitle", { name: item.recipientName })} emptyText={t(dict, "recap.empty")} />`.

The open link uses `item.locale` deliberately: it points at the invitation, which lives at its own locale prefix. The "new invitation" CTA uses the viewer's locale, because that is what the creator is currently working in.

- [ ] **Step 5: Build and eyeball all three locales**

Run: `npm run build`
Expected: succeeds.

Run `npm run dev` and open `/he`, `/ru`, `/en`. Expected: title, subtitle, button, status badges and dates all translated; `/he` is RTL and the other two LTR; the switcher moves between them and the URL prefix changes.

- [ ] **Step 6: Check phone width**

At 390px in devtools, on each locale, run in the console:

```js
document.body.scrollWidth <= window.innerWidth
```

Expected: `true` in all three.

- [ ] **Step 7: Prepared commit (deferred)**

```bash
git add "src/app/[locale]/page.tsx" src/components/DashboardList.tsx src/components/Mascot.tsx src/components/RecapCard.tsx
git commit -m "feat(i18n): translate the dashboard and add locale badges"
```

---

### Task 10: Translate the builder

**Files:**
- Modify: `src/app/[locale]/new/page.tsx`, `src/components/BuilderForm.tsx`

**Interfaces:**
- Consumes: `getDictionary`, `t`, `isLocale`, `Locale`, `defaultGateQuestion`, `defaultQuestions`, `FieldError`.
- Produces: `export function BuilderForm({ locale, dict }: { locale: Locale; dict: Dictionary })`.

- [ ] **Step 1: Rewrite the builder page**

`src/app/[locale]/new/page.tsx`:

```tsx
import Link from "next/link";
import { notFound } from "next/navigation";
import { BuilderForm } from "@/components/BuilderForm";
import { LanguageSwitcher } from "@/components/LanguageSwitcher";
import { Sparkles } from "@/components/Sparkles";
import { isLocale } from "@/lib/i18n/locales";
import { getDictionary, t } from "@/lib/i18n/t";

export default async function NewInvitationPage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  if (!isLocale(locale)) notFound();
  const dict = getDictionary(locale);

  return (
    <>
      <Sparkles />
      <main className="relative z-10 mx-auto w-full max-w-2xl px-4 py-10">
        <div className="mb-4 flex justify-end">
          <LanguageSwitcher locale={locale} dict={dict} />
        </div>

        <div className="mb-6 flex flex-wrap items-center justify-between gap-3">
          <h1 className="text-3xl font-bold text-rose-deep">{t(dict, "builder.title")}</h1>
          <Link
            href={`/${locale}`}
            className="rounded-2xl border-2 border-blush-deep bg-white px-4 py-2 font-bold text-rose-ink/70 transition hover:bg-blush"
          >
            {t(dict, "builder.back")}
          </Link>
        </div>
        <BuilderForm locale={locale} dict={dict} />
      </main>
    </>
  );
}
```

Switching language here re-mounts `BuilderForm` with a new `locale` key, which
resets the draft to that locale's seeds. That is the wanted behaviour: the
creator picks a language, then writes.

- [ ] **Step 2: Rewrite `BuilderForm`**

In `src/components/BuilderForm.tsx`:

Replace the defaults import and add the i18n ones:

```tsx
import {
  MAX_OPTIONS,
  MIN_OPTIONS,
  defaultGateQuestion,
  defaultQuestions,
  emptyQuestion,
} from "@/lib/defaults";
import type { Locale } from "@/lib/i18n/locales";
import { t, type Dictionary, type MessageKey } from "@/lib/i18n/t";
import type { Draft, DraftErrors, DraftQuestion, FieldError, MascotKind } from "@/lib/types";
```

Delete the module-level `MASCOT_LABELS` constant — mascot names come from the dictionary now.

Change `FieldError` (the local component — rename it to avoid clashing with the imported type) to render a code:

```tsx
function FieldErrorText({ error, dict }: { error?: FieldError; dict: Dictionary }) {
  if (!error) return null;
  return (
    <p className="mt-1 text-sm font-bold text-rose-deep">
      {t(dict, error.code, error.params)}
    </p>
  );
}
```

Update all nine `<FieldError message={...} />` call sites to
`<FieldErrorText error={...} dict={dict} />`.

Signature and initial state:

```tsx
export function BuilderForm({ locale, dict }: { locale: Locale; dict: Dictionary }) {
  const [draft, setDraft] = useState<Draft>({
    recipientName: "",
    mascot: null,
    gateQuestion: defaultGateQuestion(locale),
    questions: defaultQuestions(locale),
    locale,
  });
```

`serverError` becomes a message key instead of a sentence:

```tsx
  const [serverError, setServerError] = useState<MessageKey | null>(null);
```

and in `generate()`:

```tsx
      if (!response.ok) {
        const payload = (await response.json().catch(() => null)) as { code?: string } | null;
        setServerError(
          payload?.code && payload.code in dict
            ? (payload.code as MessageKey)
            : "api.createFailed"
        );
        return;
      }
```

with the `catch` branch setting `setServerError("api.createFailed")`, and the
render becoming `{serverError && <p ...>{t(dict, serverError)}</p>}`.

The share link and the preview link both carry the invitation's locale, which is
the locale the form was filled in:

```tsx
  const shareLink =
    createdId && typeof window !== "undefined"
      ? `${window.location.origin}/${locale}/invite/${createdId}`
      : null;
```

```tsx
          <Link href={`/${locale}/invite/${createdId}`} ...>
            {t(dict, "builder.done.preview")}
          </Link>
          <Link href={`/${locale}`} ...>
            {t(dict, "builder.done.back")}
          </Link>
```

Then swap each remaining literal for its key:

| Location | Key |
|---|---|
| success `<h2>` | `builder.done.title` |
| success `<p>` | `builder.done.subtitle` |
| copy button | `builder.done.copied` / `builder.done.copy` |
| the note `<p>` | `builder.done.note` |
| success mascot `label` | `mascot.bear` / `mascot.penguin` from `draft.mascot ?? "BEAR"` |
| name `<Label>` | `builder.name.label` |
| name `placeholder` | `builder.name.placeholder` |
| mascot `<Label>` | `builder.mascot.label` |
| mascot button text and `Mascot` `label` | `mascot.bear` / `mascot.penguin` |
| gate `<Label>` | `builder.gate.label` |
| questions `<h2>` | `builder.questions.title` |
| question `placeholder` | `builder.question.placeholder` |
| up/down/delete `aria-label`s | `builder.moveUp` / `builder.moveDown` / `builder.removeQuestion` |
| option `placeholder` | `builder.option.placeholder` with `{ n: optionIndex + 1 }` |
| option delete `aria-label` | `builder.removeOption` |
| add-option button | `builder.addOption` |
| add-question button | `builder.addQuestion` |
| submit button | `builder.submitting` / `builder.submit` |

For the mascot grid, replace `Object.keys(MASCOT_LABELS)` with a local list and a
key lookup:

```tsx
const MASCOT_KINDS: MascotKind[] = ["BEAR", "PENGUIN"];
const mascotKey = (kind: MascotKind): MessageKey =>
  kind === "BEAR" ? "mascot.bear" : "mascot.penguin";
```

- [ ] **Step 3: Force `BuilderForm` to remount on a language switch**

In `src/app/[locale]/new/page.tsx`, give it a key so the seeded draft follows the switch:

```tsx
        <BuilderForm key={locale} locale={locale} dict={dict} />
```

- [ ] **Step 4: Build**

Run: `npm run build`
Expected: succeeds.

- [ ] **Step 5: Exercise the builder in each locale**

With `npm run dev`, for each of `/he/new`, `/ru/new`, `/en/new`:

1. Every label, placeholder and button is in that language, and the three seeded questions are too.
2. Press the submit button with the form empty. Expected: inline errors in that language on the name and the mascot.
3. Type a 41-character name. Expected: the too-long error naming 40.
4. Delete every question. Expected: the "at least one question" error.
5. Fill it in properly and submit. Expected: the success card, and the share link contains that locale's prefix (e.g. `http://localhost:3000/ru/invite/<id>`).
6. At 390px, `document.body.scrollWidth <= window.innerWidth` is `true`.

- [ ] **Step 6: Prepared commit (deferred)**

```bash
git add "src/app/[locale]/new/page.tsx" src/components/BuilderForm.tsx
git commit -m "feat(i18n): translate the builder and prefix generated links"
```

---

### Task 11: Translate the invite flow, with the invitation's locale winning

**Files:**
- Modify: `src/app/[locale]/invite/[token]/page.tsx`, `src/components/InviteFlow.tsx`

**Interfaces:**
- Consumes: `InviteView` (now carrying `locale`), `getDictionary`, `t`, `isLocale`, `DEFAULT_LOCALE`, `PLEA_KEYS`.
- Produces: `export function InviteFlow({ invite, dict }: { invite: InviteView; dict: Dictionary })`. The page redirects when the URL locale disagrees with the invitation's.

- [ ] **Step 1: Rewrite the invite page**

`src/app/[locale]/invite/[token]/page.tsx`:

```tsx
import { notFound, redirect } from "next/navigation";
import { CuteCard } from "@/components/CuteCard";
import { InviteFlow } from "@/components/InviteFlow";
import { Mascot } from "@/components/Mascot";
import { RecapCard } from "@/components/RecapCard";
import { Sparkles } from "@/components/Sparkles";
import { DEFAULT_LOCALE, isLocale, type Locale } from "@/lib/i18n/locales";
import { getDictionary, t, type Dictionary } from "@/lib/i18n/t";
import { prisma } from "@/lib/prisma";
import type { InviteView, MascotKind, RecapItem } from "@/lib/types";

export const dynamic = "force-dynamic";

function MissingInvitation({ dict }: { dict: Dictionary }) {
  return (
    <CuteCard top={<Mascot kind="PENGUIN" mood="idle" size={170} label={t(dict, "mascot.penguin")} />}>
      <h1 className="text-center text-2xl font-bold text-rose-deep">
        {t(dict, "invite.missing.title")}
      </h1>
      <p className="mt-3 text-center text-rose-ink/70">{t(dict, "invite.missing.text")}</p>
    </CuteCard>
  );
}

export default async function InvitePage({
  params,
}: {
  params: Promise<{ locale: string; token: string }>;
}) {
  const { locale, token } = await params;
  if (!isLocale(locale)) notFound();

  const invitation = await prisma.invitation.findUnique({
    where: { id: token },
    include: {
      questions: {
        orderBy: { order: "asc" },
        include: { options: { orderBy: { order: "asc" } } },
      },
      answers: { include: { selectedOption: true } },
    },
  });

  // A missing invitation has no locale of its own, so the URL's is all we have.
  if (!invitation) {
    return (
      <>
        <Sparkles />
        <main className="relative z-10 grid min-h-screen place-items-center px-4 py-10">
          <MissingInvitation dict={getDictionary(locale)} />
        </main>
      </>
    );
  }

  // The invitation's language wins: its content can't be translated, so the
  // chrome has to come to it rather than the other way round.
  const owned: Locale = isLocale(invitation.locale) ? invitation.locale : DEFAULT_LOCALE;
  if (owned !== locale) redirect(`/${owned}/invite/${token}`);

  const dict = getDictionary(owned);

  return (
    <>
      <Sparkles />
      <main className="relative z-10 grid min-h-screen place-items-center px-4 py-10">
        {invitation.status === "ANSWERED" ? (
          <AnsweredRecap
            recipientName={invitation.recipientName}
            mascot={invitation.mascot}
            items={recapItems(invitation)}
            dict={dict}
          />
        ) : (
          <InviteFlow invite={toInviteView(invitation, token, owned)} dict={dict} />
        )}
      </main>
    </>
  );
}
```

`toInviteView` gains the locale:

```tsx
function toInviteView(
  invitation: LoadedInvitation,
  token: string,
  locale: Locale
): InviteView {
  return {
    token,
    locale,
    recipientName: invitation.recipientName,
    ...
  };
}
```

and `AnsweredRecap` takes `dict`, using `invite.answered.title`,
`invite.answered.text` with `{ name: recipientName }`, the mascot label key, and
`RecapCard`'s new required `title={t(dict, "recap.title")}` /
`emptyText={t(dict, "recap.empty")}`.

`LoadedInvitation` does not need a `locale` field — the page reads
`invitation.locale` directly from the Prisma result before narrowing.

- [ ] **Step 2: Rewrite `InviteFlow`**

In `src/components/InviteFlow.tsx`:

Add to the imports:

```tsx
import { PLEA_KEYS } from "@/lib/dodge";
import { t, type Dictionary, type MessageKey } from "@/lib/i18n/t";
```

Signature: `export function InviteFlow({ invite, dict }: { invite: InviteView; dict: Dictionary })`.

`submitError` becomes a key, exactly like the builder's:

```tsx
  const [submitError, setSubmitError] = useState<MessageKey | null>(null);
```

```tsx
      if (!response.ok) {
        const payload = (await response.json().catch(() => null)) as { code?: string } | null;
        setSubmitError(
          payload?.code && payload.code in dict
            ? (payload.code as MessageKey)
            : "api.saveFailed"
        );
        return;
      }
```

with the `catch` setting `"api.saveFailed"` and the render using
`{t(dict, submitError)}`.

Resolve the pleas once and hand them to `DodgeButton`:

```tsx
  const pleas = PLEA_KEYS.map((key) => t(dict, key));
```

Then the three stages:

- Gate: mascot gets `label={t(dict, invite.mascot === "BEAR" ? "mascot.bear" : "mascot.penguin")}`; the intro `<p>` becomes `{t(dict, "invite.gate.intro", { name: invite.recipientName })}`; `<HeartButton label={t(dict, "invite.yes")} ... />`; `<DodgeButton label={t(dict, "invite.no")} pleas={pleas} onDodge={...} />`.
- Questions: the same mascot label; the retry button becomes `{t(dict, "invite.retry")}`.
- Finale: the same mascot label; `<h2>` is `invite.finale.title`; the `<p>` is `invite.finale.text` with `{ name: invite.recipientName }`; `RecapCard` gets `title={t(dict, "recap.title")}` and `emptyText={t(dict, "recap.empty")}`.

Because the mascot label is needed in all three stages, hoist it:

```tsx
  const mascotLabel = t(dict, invite.mascot === "BEAR" ? "mascot.bear" : "mascot.penguin");
```

- [ ] **Step 3: Build and type check**

Run: `npm run build`
Expected: succeeds with no remaining type errors anywhere.

- [ ] **Step 4: Walk a full invitation in each locale**

For each of `he`, `ru`, `en`: create an invitation at `/{locale}/new`, open the share link, and check that the intro, the gate buttons, the pleas, the retry copy and the finale are all in that language, with `he` RTL and the others LTR.

- [ ] **Step 5: Verify the locale redirect**

Take the id of a Hebrew invitation and visit `http://localhost:3000/en/invite/<id>`.
Expected: the URL becomes `/he/invite/<id>` and the card is Hebrew and RTL. Repeat with a Russian invitation opened under `/he/` — expected redirect to `/ru/...`.

- [ ] **Step 6: Confirm the dodge button still behaves**

On the gate in each locale: move the cursor toward the "no" button — it bolts away before being hovered. Click it several times — it shrinks and the plea escalates in that language, and after 5+ catches the last plea repeats and the button never disappears. Tab to it — it hops.

- [ ] **Step 7: Prepared commit (deferred)**

```bash
git add "src/app/[locale]/invite" src/components/InviteFlow.tsx
git commit -m "feat(i18n): translate the invite flow; invitation locale wins over the URL"
```

---

### Task 12: API edges, docs, and full verification

**Files:**
- Modify: `CLAUDE.md`, `README.md`

- [ ] **Step 1: Run the whole unit suite**

Run: `npm test`
Expected: PASS — dodge, validation, and i18n.

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: succeeds.

- [ ] **Step 3: Re-check the API edges**

With `npm run dev`, using a token from an answered invitation:

```bash
curl -si -X POST http://localhost:3000/api/invitations/UNKNOWN/answers \
  -H "Content-Type: application/json" -d '{"answers":[]}' | head -8

curl -si -X POST "http://localhost:3000/api/invitations/<answered-id>/answers" \
  -H "Content-Type: application/json" -d '{"answers":[]}' | head -8

curl -si -X POST http://localhost:3000/api/invitations \
  -H "Content-Type: application/json" -d '{"recipientName":"","mascot":null,"gateQuestion":"","questions":[],"locale":"en"}' | head -12
```

Expected: `404 {"code":"api.notFound"}`; `409 {"code":"api.alreadyAnswered"}`; `400` with an `errors` object whose values are `{"code":"error.name.required"}`-shaped, not sentences.

Also confirm a locale round-trip:

```bash
curl -s -X POST http://localhost:3000/api/invitations \
  -H "Content-Type: application/json" \
  -d '{"recipientName":"Maya","mascot":"BEAR","gateQuestion":"Will you?","questions":[{"id":"q1","text":"Food?","options":["Sushi","Pizza"]}],"locale":"en"}'
```

Expected: `201 {"id":"..."}`, and `/he/invite/<id>` redirects to `/en/invite/<id>`.

- [ ] **Step 4: Update `CLAUDE.md`**

Replace the first "Hard rules" bullet — the one starting `**Hebrew only, RTL.**` — with:

```markdown
- **Three languages: `he`, `ru`, `en`.** All chrome resolves through
  `src/lib/i18n/` — never put a user-facing literal in a component or route.
  `he` is `DEFAULT_LOCALE` *and* the source-of-truth dictionary: `ru.ts` and
  `en.ts` are typed `: Dictionary`, so a missing key is a build error.
- **Direction is per locale**, not global: `DIR` in `src/lib/i18n/locales.ts`
  (`he` → `rtl`, `ru`/`en` → `ltr`), applied by `src/app/[locale]/layout.tsx`.
  Check both directions after any layout change.
- **Creator content is never translated** — `recipientName`, `gateQuestion`,
  question texts and option labels are stored verbatim. `seed.*` in the
  dictionaries are only the builder's pre-fills.
- **Detection precedence** is `resolveLocale()`: URL prefix → `sayyes_locale`
  cookie → `Accept-Language` → `he`. It runs in `src/middleware.ts`, i.e. before
  render, so there is no wrong-language flash. The matcher must keep excluding
  `/api`.
- **An invitation owns its locale.** `Invitation.locale` is stored at creation,
  and `/{locale}/invite/{token}` redirects to the invitation's locale so chrome
  and content always agree. No switcher on the invite page.
```

In the RTL-gotcha bullet, note that both directions now render, so physical-side
utilities are a bug in one of them — prefer logical properties (`ms-`, `me-`,
`ps-`, `pe-`).

In the **Architecture** section, add `src/lib/i18n/` to the pure-logic list:

```markdown
  - `i18n/locales.ts` — `resolveLocale()` (path → cookie → header → `he`),
    `negotiate()` (q-weighted `Accept-Language`), `localeFromPath`, `swapLocale`.
  - `i18n/t.ts` + `i18n/dictionaries/*` — the string tables; `tests/i18n.test.ts`
    asserts identical key sets and identical placeholders across all three.
```

and note that every page lives under `src/app/[locale]/`, that this layout is the
root layout, and that `validateDraft` now returns error **codes**.

In **Visual system**, replace the Varela Round reference: the font is Rubik
(`hebrew`, `latin`, `cyrillic`) exposed as `--font-app`.

In **Verification**, add the per-locale click-through and the redirect checks
from Task 11 Step 5.

- [ ] **Step 5: Update `README.md`**

Add a "Languages" section covering: the three locales and their URL prefixes; the
detection precedence table; that the switcher lives on the dashboard and builder
but not the invite page and why; that invitation content is stored in the
creator's language and never translated; and how to add a string (add the key to
`he.ts`, then `ru.ts` and `en.ts` — the type checker will point at whatever is
missing). Mention `Invitation.locale` in whatever schema description the README
already has.

- [ ] **Step 6: Full manual click-through, all three locales**

For each of `he`, `ru`, `en`: dashboard → switcher → builder (including
validation errors) → link → gate (try to catch the "no" button) → questions →
finale → reload the link for the read-only recap → back to the dashboard recap.
Then: a fresh incognito window with the browser set to Russian lands on `/ru`;
`/de` is a 404; the dashboard shows the right locale badge for each invitation.

- [ ] **Step 7: Screenshots**

Any screenshots go to the scratchpad directory, never the repo root.

- [ ] **Step 8: Prepared commit (deferred)**

```bash
git add CLAUDE.md README.md
git commit -m "docs: document the he/ru/en localization system"
```

---

## Self-Review

**Spec coverage**

| Spec section | Task |
|---|---|
| Two string classes; seeds are translated | 2, 5 |
| Recipient sees the invitation's locale | 6, 11 |
| Path prefix `/he` `/ru` `/en` | 7 |
| Rubik font | 7 |
| Hand-rolled `src/lib/i18n/` | 1, 2 |
| Detection precedence + middleware | 1, 7 |
| `locales.ts` interface | 1 |
| Typed dictionaries + `t()` | 2 |
| Error codes replace sentences | 3, 6 |
| Plea keys | 4 |
| Locale-aware defaults | 5 |
| Routing / root layout / `generateStaticParams` / metadata | 7 |
| `Invitation.locale` + `db:push` | 6 |
| Invite-page redirect + no switcher there | 11 |
| `LanguageSwitcher` | 8 |
| Visual system, dashboard badge, 390px | 7, 9 |
| Error handling (`/de` 404, bad header, bad DB locale) | 7, 9, 11 |
| Tests: negotiate, resolveLocale, path helpers, integrity | 1, 2 |
| Updated validation + dodge tests | 3, 4 |
| Manual per-locale click-through and API edges | 12 |
| CLAUDE.md + README | 12 |

**Type consistency:** `Dictionary` is `Record<keyof typeof he, string>` (not
`typeof he`, which `as const` would make unassignable) and is used consistently
from Task 2 onward. `FieldError` is the `{ code, params }` type from Task 3;
`BuilderForm`'s local error component is renamed `FieldErrorText` to avoid
shadowing it. `pleaKeyForCatches` is the only plea function after Task 4 —
`pleaForCatches` and `PLEAS` are gone, and Task 4 Step 5 updates the one caller.
`Mascot`'s `label` and `RecapCard`'s `title`/`emptyText` become required in
Task 9, and Tasks 9–11 update every call site.
